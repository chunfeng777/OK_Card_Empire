# OK PACK 开包动画

独立预览地址：`/pack-opening`。首页、盲盒列表、详情页的购买确认后共用此组件。

## 组件

- `src/components/pack-opening/PackOpening.vue`：流程状态、按钮、鼠标/触屏封口手势、键盘交互、对话框焦点管理。
- `PackScene.vue`：Three.js 环形轮盘、洗牌旋转、射线选包、卡包放大、封条分离、资源释放。
- `OpeningVideo.vue`：后续视频播放，包含自动播放受限时的播放按钮、失败提示和跳过入口。
- `types.ts`：阶段、封口投影坐标、默认封面路径。

封面直接使用用户提供的原图，存放于 `public/images/pack-opening/ok-pack.jpg`。

## 交互

左右拖动、滚轮或左右箭头旋转 → 直接点击任意可见卡包（洗牌可选）→ 正面展示 → 从左向右划开距离顶部约四分之一处的开口 → 上部包装从左到右卷起并飞出 → 全屏视频。正反面均使用同一封面；洗牌保持卡包高度与倾角不变，仅平稳绕轮盘旋转。

白色实线从左到右循环提示。划开动作需从左端开始，单次向右覆盖至少 82% 卡包宽度；右往左、中间起划、点击、短距离拖动、明显偏离开口均不会打开。键盘聚焦封口后可重复按右方向键完成。减少动态效果模式中提示线静止。

## 接入后续视频

已接入用户提供的「飞书20260911-162750.mp4」，存放在 `public/videos/ok-pack-reveal.mp4`。视频为 1080×1920、约 15 秒，保留原有 H.264 画面和 AAC 声音，仅将 MP4 索引前移以便浏览器更快开始播放。默认供独立预览与购买流程使用；购买流程仍可通过 `VITE_PACK_OPENING_VIDEO_URL` 覆盖。

划开后视频铺满浏览器视口，隐藏标题和步骤栏；使用等比例 cover 裁切适配横屏和竖屏，不拉伸画面。独立预览中，视频结束或跳过后可再体验一次；购买流程则进入现有结果页。显式传入空视频地址仍可停留在衔接页。`opened` 事件返回 `{ packIndex, count }`，购买流程中视频结束或跳过触发 `complete`。浏览器若限制有声自动播放，会显示播放按钮。

抽取次数仍由原购买流程传入；十连抽共用一段开包动画，结果页显示原来的十张结果。动画不生成奖品或改变概率。现有余额及结果是前端演示逻辑，没有新增后端抽奖或订单写入。

关闭购买后的动画会显示原结果页，不再次扣除余额。关闭再重新选择商品时重置购买弹窗阶段。

组件按需加载；卸载时停止渲染循环、移除事件、释放几何体/材质/纹理并恢复滚动。支持减少动态效果设置，WebGL/纹理失败可重新加载。
